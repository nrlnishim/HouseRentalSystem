<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="angel.example.houserental">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".rented"></activity>
        <activity android:name=".updaterent" />
        <activity android:name=".booked" />
        <activity android:name=".signup" />
        <activity android:name=".houseinfo" />
        <activity android:name=".housedetail2" />
        <activity android:name=".housedetail" />
        <activity android:name=".fillinfo" />
        <activity android:name=".choose" />
        <activity android:name=".welcome3" />
        <activity android:name=".welcome2" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <meta-data
        android:name="firebase_analytics_collection_deactivated"
        android:value="true" />

    <supports-screens
        android:largeScreens="true"
        android:normalScreens="true"
        android:smallScreens="true"
        android:xlargeScreens="true" />

    <dist:module dist:instant="true" />

</manifest>